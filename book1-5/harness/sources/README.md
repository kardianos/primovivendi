# Sources

Store durable bibliographic keys here. Attack `source_ref` fields should point at keys in `catalog.yaml` or at stable citations.

Do not dump illegal copies of copyrighted books. Short quotes for criticism/review in attack files are fine; prefer links and page references for long works.

For book1-4 research reuse, see `/home/d/code/po/book1-4/ref/`.
